{
  "name": "procure-network-assistant",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build-win": "electron-builder build --win"
  },
  "build": {
    "appId": "com.electron.app",
    "linux": {
      "target": [
        "AppImage",
        "tar.gz"
      ]
    },
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "artifactName": "electron_app.exe"
    }
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^22.0.0",
    "electron-builder": "^23.6.0"
  },
  "dependencies": {
    "auto-launch": "^5.0.5",
    "electron-json-storage": "^4.6.0",
    "mongoose": "^6.8.3",
    "systeminformation": "^5.17.3"
  }
}
